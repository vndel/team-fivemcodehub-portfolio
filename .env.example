# Discord bot token — used only by `npm run seed`, never shipped to the browser.
# Create one at https://discord.com/developers/applications
DISCORD_BOT_TOKEN=

# Invite code for the server card (the part after discord.gg/)
DISCORD_INVITE_CODE=5cc
